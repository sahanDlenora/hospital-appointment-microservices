spring:
  application:
    name: schedule-service

  datasource:
    url: jdbc:mysql://localhost:3306/Schedule_DB?createDatabaseIfNotExist=true&useSSL=false
    username: root
    password: Thilina1234#
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update

server:
  port: 8088

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    ip-address: 127.0.0.1


